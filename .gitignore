# Build output
bin/
obj/
[Dd]ebug/
[Rr]elease/

# Visual Studio
.vs/
*.user

# Certificates & key material — never commit
Cert/
.didtoolbox/
*.pfx
*.pem
*.key
*.crt
*.p12
docker/.env

# DID toolbox (43 MB JAR, download see README)
tools/didtoolbox.jar
tools/didtoolbox-stderr.txt

# Local deployment files with real values — the repo only contains templates
# (proxy/Caddyfile.example; didlog.jsonl is created by the DID toolbox)
proxy/Caddyfile
didlog.jsonl

# Claude-local files
.claude/
